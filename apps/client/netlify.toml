[build]
  base = "apps/client"
  command = "npm install --legacy-peer-deps --no-audit --no-fund && npx expo export --platform web"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

